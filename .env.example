# =============================================================================
# POLYMARKET MONITOR - CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values
# The application will use these environment variables for configuration


# =============================================================================
# ALERTING - Discord (Optional but Recommended)
# =============================================================================
# Get a webhook URL from Discord:
# 1. Go to Server Settings > Integrations > Webhooks
# 2. Create a webhook and copy the URL
# Leave empty to disable Discord alerts (will only print to console/logs)
DISCORD_WEBHOOK_URL=


# =============================================================================
# ALERTING - Email (Optional)
# =============================================================================
# Email alerts via SMTP (e.g., Gmail, Outlook)
# Set to "false" to disable email alerts entirely
EMAIL_ENABLED=true

# SMTP server settings (Gmail example: smtp.gmail.com, port 587)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your email credentials
# For Gmail: Use an App Password, not your regular password
# https://support.google.com/accounts/answer/185833
SMTP_USER=
SMTP_PASSWORD=

# Email address to receive alerts
ALERT_EMAIL=


# =============================================================================
# MONITORING THRESHOLDS
# =============================================================================
# Only monitor markets with probability below this threshold
# Example: 0.05 = 5% probability or lower
# Range: 0.01 to 0.99
PROBABILITY_THRESHOLD=0.05

# Minimum trade size to trigger alerts (in USD)
# This filters trades on the API server side (more efficient)
# Example: 50000 = $50,000
TRADE_SIZE_MIN=50000

# Maximum trade size to monitor (in USD)
# This filters trades on the client side
# Example: 10000000 = $10,000,000 (effectively no limit for most trades)
TRADE_SIZE_MAX=10000000

# Legacy threshold setting (keep same as TRADE_SIZE_MIN)
TRADE_SIZE_THRESHOLD=50000


# =============================================================================
# API POLLING INTERVALS (Ultra Responsive Configuration)
# =============================================================================
# How often to check for new trades (in seconds)
# Ultra Responsive: 4 seconds (900 API calls/hour)
# Balanced: 10-15 seconds (240-360 calls/hour)
# Conservative: 60 seconds (60 calls/hour)
# Free tier limit: 1,000 calls/hour
POLL_INTERVAL_SECONDS=4

# How often to refresh the list of markets (in seconds)
# Ultra Responsive: 360 seconds (6 minutes, 10 calls/hour)
# Balanced: 120-300 seconds (12-30 calls/hour)
# Conservative: 300 seconds (5 minutes, 12 calls/hour)
MARKET_REFRESH_SECONDS=360


# =============================================================================
# API LIMITS
# =============================================================================
# Maximum number of trades to fetch per API call
# Default: 1000 (recommended)
TRADES_API_LIMIT=1000

# Maximum number of markets to fetch per API call
# Default: 2000 (recommended)
# Warning: If you have exactly this many markets, you might be hitting a limit
MARKETS_API_LIMIT=2000


# =============================================================================
# MARKET FILTERING
# =============================================================================
# Comma-separated list of market categories to monitor
# Options: sports, politics, other
# Example: "sports,politics" monitors both sports and politics markets
# Example: "sports" monitors only sports markets
MARKET_CATEGORIES=sports,politics


# =============================================================================
# DATABASE
# =============================================================================
# Path to SQLite database file
# Default: polymarket_monitor.db in the current directory
# The database will be created automatically if it doesn't exist
DATABASE_PATH=polymarket_monitor.db


# =============================================================================
# CURRENT CONFIGURATION SUMMARY
# =============================================================================
# With the defaults above:
# - Trades checked every 4 seconds (900 calls/hour)
# - Markets refreshed every 6 minutes (10 calls/hour)
# - Total API usage: ~910 calls/hour (91% of 1,000/hour free tier limit)
# - Trade detection latency: 4 seconds
# - Monitoring trades: $50k - $10M on sports/politics markets <5% probability
